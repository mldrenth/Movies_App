import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    image: {
        flex: 1,
        resizeMode: 'contain',
        height: 100,
        width: 200,
        borderRadius: 10,
        marginLeft: 10,
      }

});

export default styles;